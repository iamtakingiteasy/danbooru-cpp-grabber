*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SPECS*SEPCS*SPECS*

background:

class Application; 
class DCPPG : public Application {
	  class Logger;
	  class Config;
	  class Modules;
	  class Options;
}


001. int main() is called
002. 